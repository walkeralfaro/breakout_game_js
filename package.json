{
  "name": "04_breakout",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "vite",
    "build": "npm run clean && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^5.1.4"
  }
}
